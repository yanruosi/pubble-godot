---
description: pubble 家里端 — 架构与 UI 占位约束
alwaysApply: true
---

硬性约束（违反视为不合格）

- **不得擅自**新增/删除 **主场景树结构**（重组 `main_ui`、改 SafeArea 子节点层级、大面积换父节点）——除非用户在对话中 **明确写下**「可以改场景结构」。  
- **不得擅自**把占位 UI 当成最终稿：全局换配色、换字体、改导航信息架构，须先与用户确认。  
- **允许且鼓励**的 UI 调整：在已有节点上改 **位置、尺寸、theme 颜色、导出变量、卡堆 `BASE_*` / `LAYER_*`** 等「参数级」微调。  
- **数据与职责**：章节/条件继续用 **JSON**；`chapter_manager` 只加载数据；`condition_checker` 只暴露 **`is_condition_met`**；持久化只走 **`save_manager`**。  
- **禁止代码冗余**：新逻辑优先接到现有 Singleton/现有脚本；禁止复制一整份同类校验或存档读写。  
- **禁止私增枚举替代已定方案**：例如 **不要**引入 `CardState` enum；状态用 **int 0/1/2**。  
- **信息不足**：最多 **3 个**澄清问题；不要长篇假设后大改。

<!-- 与 ai-context/RULE_pubble_home.md 保持一致；改一处时请同步另一处 -->
